import type { QaItem } from "./types";

/**
 * Published academic Q&A (English). Curated manually by the author from
 * Google Form responses; see zh.ts for the workflow. Never commit reader
 * emails or any personal data without consent. Set published: false to hide.
 */
export const qaEn: QaItem[] = [];
